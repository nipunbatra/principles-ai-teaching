# YOLO Architecture

direction: down

title: {
  label: YOLO Architecture
  near: top-center
  shape: text
  style.font-size: 24
  style.bold: true
}

input: {
  label: |md
    **Input**
    448×448×3
    (RGB Image)
  |
  shape: rectangle
  style.fill: "#E3F2FD"
  style.stroke: "#2E86AB"
  style.stroke-width: 2
}

backbone: {
  label: |md
    **Backbone (Feature Extractor)**
    24 Convolutional Layers

    Extract visual features:
    edges → textures → shapes → parts → objects
  |
  shape: rectangle
  style.fill: "#BBDEFB"
  style.stroke: "#1976D2"
  style.stroke-width: 2
}

fc: {
  label: |md
    **Detection Head**
    2 Fully Connected Layers

    4096 → 7×7×30
  |
  shape: rectangle
  style.fill: "#C8E6C9"
  style.stroke: "#388E3C"
  style.stroke-width: 2
}

output: {
  label: |md
    **Output Tensor**
    7 × 7 × 30
  |
  shape: rectangle
  style.fill: "#FFF9C4"
  style.stroke: "#F9A825"
  style.stroke-width: 2
}

decode: {
  direction: right

  boxes: {
    label: |md
      **2 Boxes per cell**
      [x, y, w, h, conf] × 2
      = 10 values
    |
    shape: rectangle
    style.fill: "#FFECB3"
  }

  class_probs: {
    label: |md
      **20 Class probs**
      P(dog), P(cat), ...
      = 20 values
    |
    shape: rectangle
    style.fill: "#FFECB3"
  }

  total: {
    label: |md
      **Total: 30 per cell**
      7×7×30 = 1470
    |
    shape: rectangle
    style.fill: "#FFE082"
  }
}

input -> backbone: ""
backbone -> fc: ""
fc -> output: ""
output -> decode: "Decode"
